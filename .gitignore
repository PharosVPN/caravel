# gomobile build artifacts — rebuildable via ./build-bindings.sh, never committed.
# This is the CORE library repo; caravel-android / caravel-ios are SEPARATE repos.
dist/

# Go
go/go.sum.bak
*.test
# the headless reference CLI compiled in place (rebuild with `go build ./cmd/caravel`)
go/caravel
go/cmd/caravel/caravel

# OS
.DS_Store
bin/
